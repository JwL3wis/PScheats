Run "Search_userPassword.ps1" you will need to specify the OU.
This generates Output.txt within this directory.
Run "Delete_userPassword.ps1" you will need to specify the DC.

Before executing the script, it is strongly recommend testing it in a controlled environment or on a small subset of computer accounts to ensure it works as expected and has no unintended side effects. 

Modifying Active Directory attributes can have significant consequences if done incorrectly, so exercise caution. 

Always have a recent backup of your Active Directory before making any changes.